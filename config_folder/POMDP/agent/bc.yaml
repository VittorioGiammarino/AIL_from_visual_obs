
agent_name: bc

agent:
  _target_: agents.bc.BcAgent
  obs_shape: ??? # to be specified later
  action_shape: ??? # to be specified later
  device: ${device}
  lr: ${lr}
  actor_target_tau: 0.01
  use_tb: ${use_tb}
  hidden_dim: 1024
  feature_dim: ${feature_dim}
  stddev_clip: 0.3

